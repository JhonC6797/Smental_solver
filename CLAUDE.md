# Smental Solver — הנחיות לעבודה בפרויקט

פותר אוטומטי למשחק סמנטל בעברית, ולוח ויזואליזציה חי שמציג את תהליך החיפוש.

האפיון המלא: [docs/superpowers/specs/2026-09-18-semantle-live-board-design.md](docs/superpowers/specs/2026-09-18-semantle-live-board-design.md)

## עקרונות שאסור להפר

**סדר בקבצים.** כל מודול בתיקייה שמתאימה לתפקידו, ושם קובץ שאומר מה הוא עושה.
יוצרים תיקייה חדשה כשהמבנה דורש זאת, במקום לערום קבצים בשורש. אין סקריפטים
חד-פעמיים או ניסיוניים שמסתובבים בעץ בלי בית ברור.

**`solver/` לא יודע שקיים שרת.** הוא לא מייבא דבר מ-`server/`, לא מדפיס
לטרמינל, ולא יודע שקיים לוח תלת-ממדי. הוא פולט אירועים.

**`server/` לא מכיר את פנים האלגוריתם.** הוא מעביר אירועים ומוסיף מיקום.
שמות של מנגנונים אלגוריתמיים (repulsion, temperature) לא מופיעים בקוד ה-Frontend —
הוא מצייר לפי טיפוס האבחון, לא לפי שמו.

**הממשק הוא web.** React + TypeScript + Three.js. לא Streamlit, לא Dash, ולא
UI בפייתון.

**האלגוריתם עשוי להשתנות.** כל תוספת לחוזה האירועים נשאלת: האם זה נכון לכל
אלגוריתם חיפוש, או רק לנוכחי? אם רק לנוכחי — מקומה בערוץ האבחון הגנרי.

## קבצים שלא בגיט

`data/` מכיל את מודל FastText (1.23GB) ואת הארטיפקט הבנוי ממנו. לא נכנס לגיט.
בנייתו: `python scripts/build_vocab.py`.

מקור המודל:
`https://dl.fbaipublicfiles.com/fasttext/vectors-wiki/wiki.he.vec`

## הרצה

```
python run_cli.py          # הפותר בטרמינל
uvicorn server.app:app     # השרת
```

## כיול ובאגים

יש קבועים באלגוריתם שידוע שהם חשודים בכיול שגוי (הסף 45, הטמפרטורות, מעבר
המצב ב-65). הם **לא משתנים בלי מדידה** — הלוח עצמו הוא כלי המדידה. ראה את
סעיף 7 והנספח באפיון לרשימה המלאה ולממצאים שכבר אומתו.
