# Watch a real search against the live game instead of the published
# recording. Needs the FastAPI server running; see the README.
VITE_LIVE_SOLVER=true

# Only used when VITE_LIVE_SOLVER is true. Defaults to http://localhost:8000.
VITE_API_URL=http://localhost:8000
